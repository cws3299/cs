// 표현식은 값을 만들어내기 때문에 함수의 인자로 사용할 수 있다.
alert(1000 + 900 + 90 + 4);
alert("hello" + "javascript")

// 하나 혹은 여러 개의 표현식이 모여 문장을 이룬다.
// 모든 표현식은 문장이 될 수 있다.
// (보통) 문장의 끝에는 세미 콜론을 붙인다.

1000+900+90+4; // 표현식이자 문장

var name = 'Mark' // answ